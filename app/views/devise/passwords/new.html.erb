<div class="gb ut">
  <div class="uv">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'alq dj j' }) do |f| %>
      <%= devise_error_messages! %>

      <a href="/" class="l amb">
        <%= image_tag('login.png', alt: "brand") %>
      </a>

      <div class="et">
        <%= f.email_field :email, placeholder: "Email Address", autofocus: true, autocomplete: false, class: 'form-control' %>
      </div>

      <div class="amb">
        <button class="cg fp fw">Send me reset password instructions</button>
      </div>

      <footer class="apd">
        <%= link_to "Login", login_path, class: "dp" %> |
        <%= link_to "Create Account", new_registration_path(resource_name), class: "dp" %>
      </footer>
    <% end %>
  </div>
</div>
