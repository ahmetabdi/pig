<!DOCTYPE html>
<html>
   <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>Login</title>
      <link href="http://fonts.googleapis.com/css?family=Droid+Sans:400,700" rel="stylesheet" type="text/css">
      <link href="http://fonts.googleapis.com/css?family=Droid+Serif:400,700,400italic,700italic" rel="stylesheet" type="text/css">
      <link rel="stylesheet" href="http://netdna.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
      <%= stylesheet_link_tag('gnb.css') %>
      <%= stylesheet_link_tag('fonts.css') %>
      <%= stylesheet_link_tag('login.css') %>
      <%= stylesheet_link_tag('main.css') %>
      <script src="http://code.jquery.com/jquery-1.12.0.min.js"></script>
      <%= javascript_include_tag('common.js') %>
   </head>
   <body>
      <%= render 'layouts/nav' %>

      <div id="containerWrap">
         <div id="header"></div>
         <div id="contents">
            <header id="topHeader">
               <h1 class="subTitle">Log In</h1>
            </header>
            <div id="contentWrap">
               <div class="wrapLogin">
                  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                     <div class="login-input">
                        <fieldset>
                           <legend>LOG IN</legend>
                           <div class="inputGroup">
                              <%= f.label :email, 'Email Address' %>
                              <%= f.email_field :email, placeholder: "Email Address", autofocus: true, autocomplete: false %>
                           </div>
                           <div class="inputGroup wrapPassword">
                              <%= f.label :password, 'Password' %>
                              <%= f.password_field :password, autocomplete: "off", placeholder: "Password" %>
                           </div>
                           <div class="wrapAccountRemember">
                              <input type="checkbox" name="remember" id="remember" class="checkbox">
                              <%= f.check_box :remember_me, id: "remember", class: "checkbox" %>
                              <%= f.label :remember_me, "Remember me" %>
                           </div>
                           <%= render 'layouts/errors' %>
                           <div class="wrapButton">
                              <%= f.submit "LOG IN", class: "button squareSingle" %>
                           </div>

                           <div class="wrapButton">
                              <span class="box square"><a href="#" id="btnConfirm" class="button">CONTINUE</a></span>
                              <span class="bar">|</span>
                              <span class="box square" style="float: right;"><a href="http://us.ncsoft.com" class="button">CANCEL</a></span>
                           </div>

                           <div class="wrapLink">
                              <span class="link">
                                 <%= link_to "Create Account", new_session_path(resource_name), class: "button text" %>
                              </span>
                              <span class="bar"></span>
                              <span class="link">
                                 <%= link_to "Canʹt log in?", new_password_path(resource_name), class: "button text" %>
                              </span>
                           </div>
                        </fieldset>
                     </div>
                  <% end %>
               </div>
            </div>
         </div>
      </div>
   </body>
</html>

